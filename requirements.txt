# ═══════════════════════════════════════════════════════════════
# Root requirements.txt — 공통 의존성 (prompt_validate 등)
# build_kg.yml | prompt_validate.yml | validate_prompts.yml
# jv_prompt_validate.yml | pe7_e0n_validate.yml
# ═══════════════════════════════════════════════════════════════

# HTTP 클라이언트
requests>=2.31.0

# YAML 파싱 (프롬프트 메타데이터)
pyyaml>=6.0.1

# JSON 스키마 검증
jsonschema>=4.21.0

# Notion API 클라이언트
notion-client>=2.2.1

# 환경변수
python-dotenv>=1.0.0

# 날짜 처리
python-dateutil>=2.8.2

# 로깅
colorama>=0.4.6
